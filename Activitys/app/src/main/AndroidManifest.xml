<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Activitys"
        tools:targetApi="31">
        <activity
            android:name=".IMCapp.Result_IMC_Activity"
            android:exported="false"
            android:label="@string/title_activity_result_imc"
            android:theme="@style/Theme.Activitys" />
        <activity
            android:name=".IMCapp.IMCActivity"
            android:exported="false"
            android:label="@string/title_activity_IMC"
            android:theme="@style/Theme.Activitys" />
        <activity
            android:name=".MenuActivity"
            android:exported="true"
            android:label="@string/title_activity_menu"
            android:theme="@style/Theme.Activitys">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".firstApp.ResultActivity"
            android:exported="false"
            android:label="@string/title_activity_result"
            android:theme="@style/Theme.Activitys" />
        <activity
            android:name=".firstApp.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Activitys" />
    </application>

</manifest>